// src/pages/HomeContent.jsx
import React from 'react';

// Стили можно добавить позже
const styles = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '60vh', // Просто чтобы было видно
  color: '#ECEBE4'
};

export function HomeContent() {
  return (
    <div style={styles}>
    </div>
  );
}